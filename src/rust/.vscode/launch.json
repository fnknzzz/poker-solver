{
  // 使用 IntelliSense 了解相关属性。
  // 悬停以查看现有属性的描述。
  // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug unit tests in library 'poker'",
      "cargo": {
        "args": ["test", "--no-run", "--lib", "--package=poker"],
        "filter": {
          "name": "poker",
          "kind": "lib"
        }
      },
      "args": ["it_works"],
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug ignored unit tests in library 'poker'",
      "cargo": {
        "args": ["test", "--no-run", "--lib", "--package=poker"],
        "filter": {
          "name": "poker",
          "kind": "lib"
        }
      },
      "args": ["--ignored"],
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug executable 'poker'",
      "cargo": {
        "args": ["build", "--bin=poker", "--package=poker"],
        "filter": {
          "name": "poker",
          "kind": "bin"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug unit tests in executable 'poker'",
      "cargo": {
        "args": ["test", "--no-run", "--bin=poker", "--package=poker"],
        "filter": {
          "name": "poker",
          "kind": "bin"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
